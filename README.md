
  # Settings Panel UI Components

  This is a code bundle for Settings Panel UI Components. The original project is available at https://www.figma.com/design/PmZYD14iSzmyfMEJnDbyQ3/Settings-Panel-UI-Components.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  